<!-- Simple full-width example-->

<div class="page-header">
  <h1>Todo #<%= @todo.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt>Jobapp</dt>
      <dd><%= @todo.jobapp_id %></dd>

      <dt>Due at</dt>
      <dd><%= @todo.due_at %></dd>

      <dt>Action</dt>
      <dd><%= @todo.action_id %></dd>

      <dt>Contact</dt>
      <dd><%= @todo.contact_id %></dd>

      <dt></dt>
      <dd>
        <div class="btn-group">
          <a href="/todos" class="btn btn-primary">
            <i class="fa fa-chevron-left"></i>
          </a>

          <a href="/todos/<%= @todo.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>

          <a href="/delete_todo/<%= @todo.id %>" class="btn btn-danger">
            <i class="fa fa-trash-o"></i>
          </a>
        </div>
      </dd>
    </dl>

  </div>
</div>

<!-- End simple full-width example-->

<!-- Responsive panel example-->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Todo #<%= @todo.id %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">
              <dt>Jobapp</dt>
              <dd><%= @todo.jobapp_id %></dd>

              <dt>Due at</dt>
              <dd><%= @todo.due_at %></dd>

              <dt>Action</dt>
              <dd><%= @todo.action_id %></dd>

              <dt>Contact</dt>
              <dd><%= @todo.contact_id %></dd>

            </dl>
            <div class="btn-group btn-group-justified">
              <a href="/todos" class="btn btn-primary">
                <i class="fa fa-chevron-left"></i>
              </a>

              <a href="/todos/<%= @todo.id %>/edit" class="btn btn-warning">
                <i class="fa fa-edit"></i>
              </a>

              <a href="/delete_todo/<%= @todo.id %>" class="btn btn-danger">
                <i class="fa fa-trash-o"></i>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example -->
